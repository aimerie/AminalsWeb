@{
    ViewBag.Title = "APC Mini";
}

@Scripts.Render("~/bundles/apc")

@*<div>
    @for (int iRow = 0; iRow < 8; iRow++)
    {
        <div class="row py-2">
            @for (int iCol = 0; iCol < 8; iCol++)
            {
                int iCell = iRow * 8 + iCol;

                <div class="col">
                    <div class="border text-center">
                        @(iCell)
                    </div>
                </div>
            }
        </div>
    }
</div>

<hr />*@

<div class="row" data-bind="foreach: $data.Pads">
    <!-- ko if: index % 8 === 0 -->
    <div class="w-100"></div>
    <!-- /ko -->
    <div class="col py-2">
        <div class="text-center" data-bind="style: {border: '1px solid', borderColor: Color}">
            <span data-bind="text: index"></span>
        </div>
    </div>
</div>
